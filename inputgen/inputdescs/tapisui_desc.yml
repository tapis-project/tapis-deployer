tapisui_image_tag:
  description: Image tag that corresponds to the version you want to deploy
  example: dev, stage, 1.2.0
  source_vars:
    - tapisui_image_tag:

tapisui_log_level:
  description: Log level
  example: DEBUG, INFO
  source_vars:
    - tapisui_log_level:
