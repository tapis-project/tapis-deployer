apiVersion: v1
kind: Service
metadata:  
  name: pgrest-postgres
spec:
  type: ClusterIP
  selector:    
    app: pgrest-postgres
  ports:  
  - name: pgrest-postgres
    port: 5432
    targetPort: 5432

---
apiVersion: v1
kind: Service
metadata:
  name: pgrest-postgres-nodeport
spec:
  type: ClusterIP
  selector:
    app: pgrest-postgres
  ports:
  - name: pgrest-postgres
    port: 5432
    targetPort: 5432
