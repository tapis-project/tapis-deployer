apiVersion: apps/v1
kind: Deployment
metadata:
  name: nfs-server-deployment
spec:
  selector:
    matchLabels:
      app: nfs-server-deployment
  template:
    metadata:
      labels:
        app: nfs-server-deployment
    spec:
      initContainers:
      - name: create-mount-dirs
        image: busybox
        command: ["sh", "-c", "mkdir -p /nfsshare/mlhub/models /nfsshare/mlhub/datasets /nfsshare/mlhub/inference /nfsshare/mlhub/training"]
      containers:
      - name: nfs-server
        image: itsthenetwork/nfs-server-alpine:latest
        securityContext:
          privileged: true
        env:
        - name: SHARED_DIRECTORY
          value: "/nfsshare"
        ports:
        - name: nfs
          containerPort: 2049
        volumeMounts:
        - name: nfs-data
          mountPath: "/nfsshare"
        resources: {}
      volumes:
      - name: nfs-data
        persistentVolumeClaim:
          claimName: nfs-server-pvc

