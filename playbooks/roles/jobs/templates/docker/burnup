#!/bin/bash

echo "burnup jobs:"

mkdir -p {{ tapisdatadir }}/jobs

myuid=`id -u`
docker run -it --rm -v {{ tapisdatadir }}/jobs:/jobs tapis/ubutil2204:1.3.0 chown $myuid /jobs

python3 {{ tapisdir }}/admin/util/parse_skexport -c jobs -d {{ tapisdatadir }}

docker compose up -d

