apiVersion: v1
kind: Service
metadata:
  name: mlhub-traefik-service
spec:
  selector:
    app: mlhub-traefik-deployment
  ports:
    - targetPort: web
      port: 80

#--- 
#apiVersion: v1
#kind: Service
#metadata:
#  name: traefik-dashboard-service

#spec:
#  selector:
#    app: mlhub-traefik
#  type: LoadBalancer
#  ports:
#    - port: 8080
#      targetPort: dashboard
