resources:
  - ../../base

patches:
  - target:
      kind: Job
      name: mlhub-models-migrator
    patch: |-
      apiVersion: batch/v1
      kind: Job
      metadata:
        name: mlhub-models-migrator
      spec:
        template:
          spec:
            containers:
              - image: tapis/mlhub-models-migrator:staging
                imagePullPolicy: Always
                name: models-migrator
                resources: {}
                env:
                  - name: MONGO_HOST
                    value: mlhub-mongo-stateful-set-0.mlhub-mongo-headless-service.tapisstage.svc.cluster.local

