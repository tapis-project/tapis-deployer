apiVersion: v1
kind: Service
metadata:
  name: mlhub-artifact-db-service
spec:
  type: ClusterIP
  selector:
    app: mlhub-artifact-db-deployment
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017