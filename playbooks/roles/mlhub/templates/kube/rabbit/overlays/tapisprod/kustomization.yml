resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: mlhub-rabbit-deployment
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: mlhub-rabbit-deployment
      spec:
        template:
          spec:
            containers:
              - name: mlhub-rabbit
                resources:
                  requests:
                    cpu: "2"
                    memory: "4Gi"
                  limits:
                    cpu: "2"
                    memory: "4Gi"