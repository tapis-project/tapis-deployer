resources:
  - ../../base
  - ../../../site-configs/overlays/tapisdev

patches:
  - target:
      kind: Deployment
      name: mlhub-deployments-api-deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: mlhub-deployments-api-deployment
      spec:
        template:
          spec:
            containers:
              - name: deployments-api
                image: tapis/mlhub-deployments-api:dev
                imagePullPolicy: Always
                resources: {}
