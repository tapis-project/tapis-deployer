networks:
    tapis:
        name: tapis
        external: true

services:
  tapisui-service:
    container_name: tapisui
    image: {{ tapisui_image }}
    environment:
      - LOG_LEVEL={{tapisui_log_level}}



