kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: apps-postgres-vol01
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cinder-csi
  resources:
    requests:
      storage: 20Gi

