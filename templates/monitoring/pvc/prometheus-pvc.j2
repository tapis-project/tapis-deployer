---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{local_param["volume_name"]|default("prometheus-vol01")}}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{universal_param["storage_class"]|default("rbd-new")}}
  resources:
    requests:
      storage: 20Gi
