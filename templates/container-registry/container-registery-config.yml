apiVersion: v1
kind: ConfigMap
metadata: 
  name: container-registry-config
data:
  "service_site_url": "{{container-registry_service_url}}"
  "service_tenant_id": "{{container-registry_service_tenant_id}}"
  "site_id": "{{container-registry_service_site_id}}"
  "service_name": "{{container-registry_service_name}}"