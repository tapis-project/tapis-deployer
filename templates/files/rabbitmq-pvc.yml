---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: files-rabbitmq
  name: files-rabbitmq-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rbd
  resources:
    requests:
      storage: 10G