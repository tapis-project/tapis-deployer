{
    "secrets": {
        "jwtsigning":[
            {% for tenant in skadmin_initLoad_jwtsigning_tenants %}
            {
                "tenant":     "{{skadmin_tenant}}",
                "user":       "bozo",
                "secretName": "keys",
                "privateKey": "<generate-secret>",
                "kubeSecretName": "tapis-tokens-secrets",
                "kubeSecretKey":  "{{skadmin_tenant}}-privatekey"
            }{%-if not loop.last-%},{% endif %}
            {% endfor%}
        ]
    }
}
